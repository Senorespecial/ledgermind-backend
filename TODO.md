# TODO - LedgerMind scalable backend architecture (scaffold only)

- [ ] Create feature module scaffolds (Accounting, Treasury, Analytics, AI, Reporting) under `src/modules/*`.
- [ ] Add per-module controllers, services, DTOs, and domain interfaces (stub implementations).
- [ ] Add shared infrastructure/common utilities under `src/common/*`.
- [ ] Add infrastructure repository stubs under `src/infrastructure/*` that do not assume new Prisma models.
- [ ] Add facades/orchestrators for each domain (stub implementations).
- [ ] Do **not** edit existing files (including `src/app.module.ts`, `prisma/schema.prisma`).
- [ ] Do **not** run tests/build/eslint.
