# Infrastructure repositories

This directory is intended for Prisma-backed repository implementations.

NOTE: This project currently has only a `User` model in `prisma/schema.prisma`.
Repository classes added here should be implemented carefully to avoid runtime errors
until accounting/treasury/analytics domain models are introduced.
